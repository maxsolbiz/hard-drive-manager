# Frontend environment configuration
REACT_APP_BACKEND_URL=http://localhost:8000
# Set the WebSocket URL according to the actual port your websocket_server uses.
# If it uses the same port (or if it’s configured to use a specific port), update here.
REACT_APP_WEBSOCKET_URL=ws://localhost:9002
